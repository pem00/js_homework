<%- include ("header") -%>
        <h1>Űrlények és Bolygók Áttekintése</h1>
        <p>Itt láthatók a különböző űrlényfajok és az általuk kolonizált bolygók.</p>
        <a href="/">Vissza a főoldalra</a>
        <h1>Aliens & Planets Overview</h1>

<div class="tables">
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Species</th>
                <th>Technology Level</th>
            </tr>
        </thead>
        <% for (let i = 0; i < aliens.length; i++) { const alien = aliens[i] } %>
        <tbody>
            <% if (aliens.length > 0) { %>
                <% aliens.forEach(alien => { %>
                    <tr>
                        <td><%= alien.id %></td>
                        <td><%= alien.name %></td>
                        <td><%= alien.species %></td>
                        <td><%= alien.technology %></td>
                        <td>
                            <a href="/alien/<%= alien.id %>" class="btn btn-primary">View</a>
                            <a href="/alien/edit/<%= alien.id %>" class="btn btn-primary">Edit</a>
                            <form action="/alien/delete/<%= alien.id %>" method="POST" style="display: inline;">
                                <button class="btn btn-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            <% } else { %>
                <tr>
                    <td colspan="5">No aliens found</td>
                </tr>
            <% } %>
        </tbody>
    </table>
<%- include ("footer") -%>